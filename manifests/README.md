# Manifests

Small tab-separated inventories describing the managed environment.

## Files

- `emulators.tsv` — managed emulator installations
- `games.tsv` — titles with meaningful metadata
- `reshade.tsv` — deliberate ReShade installations
- `locations.example.tsv` — example path mappings

Machine-specific path records should be stored in ignored `.local.tsv` files.
